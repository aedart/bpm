# ------------------------------------------------------------------------
# "Double quote" string values
# ------------------------------------------------------------------------

foo = "bar"

zen = "      trimmed value     " ; Should automatically be trimmed

# Escaped double quotes in value
bar = "My \"double quoted\" value"

# Double quoted string values should print escaped characters.
escaped = "\tValue with\nescaped \\characters"