# ------------------------------------------------------------------------
# "Single quote" string values
# ------------------------------------------------------------------------

foo = 'bar'

zen = '      trimmed value     ' ; Should automatically be trimmed

# Escaped single quotes in value
bar = 'My \'single quoted\' value'

# Single quoted values only allow escaping of single quotes. Everything else is ignored!
other_escaped_chars = 'Other \n\t escaped characters are ignored'