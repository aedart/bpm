# ------------------------------------------------------------------------
# "Bare" values
# ------------------------------------------------------------------------

empty_value= ; An empty value must be allowed

name = John # Bare string value with comment...

age=31 ; Integer value -> will be treated as a bare string value

# A float value. This too will be treated as a bare string value
avg_level = 7.82

css_colour = #551133 ; This value will be parsed as empty!

# Both key and value is expected to be trimmed
  trimmed    =                a strange value, but valid      ; Not recommended, but valid